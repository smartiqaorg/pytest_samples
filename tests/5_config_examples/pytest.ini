[pytest]
markers =
    smoke: Main positive tests
    exception: Exception handling tests
    read: Message reading tests
    answer: Message answering tests

testpaths = tests

addopts =
    --strict-markers
    --strict-config
    -ra